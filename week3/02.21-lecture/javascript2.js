let temp = 80;
console.log(`The temperation is ${temp} degrees.`);
if (temp > 90) {
    console.log(`time to swim`)
}
let precipitation = false;
if (temp > 80 && precipitation !== "raining") {
    console.log(`time to swim`)
}
